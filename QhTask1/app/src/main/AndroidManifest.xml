<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="v.williams.qhtask1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Circles.Circles"></activity>
        <activity android:name=".ValueMeter.ValueMeter" />
        <activity android:name=".PieChart.PieChart" />
        <activity android:name=".Demos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BarGraph.BarGraph" />
        <activity android:name=".Lines.Lines" />
    </application>

</manifest>